const $=id=>document.getElementById(id);let ros,topics=[],mode='detecting',subscriptions=[],modeTimer=null;
const renderer=new MapRenderer($('map'),publishPose);
function badge(text,good=false){$('connection').textContent=text;$('connection').className='badge '+(good?'good':'bad')}
function connect(){let port=new URLSearchParams(location.search).get('rosbridge_port')||'9090';ros=new ROSLIB.Ros({url:`ws://${location.hostname}:${port}`});ros.on('connection',()=>{badge('CONNECTED',true);setup()});ros.on('close',()=>{badge('DISCONNECTED');clearSubs();setTimeout(connect,2000)});ros.on('error',()=>badge('CONNECTION ERROR'))}
function clearSubs(){subscriptions.forEach(t=>t.unsubscribe());subscriptions=[];clearInterval(modeTimer);modeTimer=null}
function topic(name,type,cb,opt={}){let t=new ROSLIB.Topic({ros,name,messageType:type,compression:opt.compression||'none',throttle_rate:opt.throttle||0});t.subscribe(cb);subscriptions.push(t);return t}
function latched(name,type,cb,opt={}){let got=false,t=topic(name,type,m=>{got=true;cb(m)},opt),retry=setInterval(()=>{if(got||!ros||!ros.isConnected){clearInterval(retry);return}t.unsubscribe();t.subscribe(m=>{got=true;cb(m)})},3000);return t}
function setup(){clearSubs();mode='detecting';ros.getTopics(result=>{topics=result.topics||[];detect();subscribeCommon();clearInterval(modeTimer);modeTimer=setInterval(pollMode,5000)})}
function pollMode(){if(!ros||!ros.isConnected)return;ros.getTopics(r=>{topics=r.topics||[];detect()})}
function detect(){let next=topics.includes('/map')?'navigation':topics.includes('/web/prep_grid')?'preparation':'detecting';if(next===mode)return;mode=next;$('mode').textContent=mode.toUpperCase();$('nav-controls').classList.toggle('hidden',mode!=='navigation');$('prep-controls').classList.toggle('hidden',mode!=='preparation');subscribeMode()}
function subscribeCommon(){latched('/localization/state','std_msgs/String',m=>$('localization').textContent=m.data);latched('/goal_executor/status','std_msgs/String',m=>$('goal-status').textContent=m.data);topic('/localization/fitness','std_msgs/Float32',m=>$('fitness').textContent=Number(m.data).toFixed(3),{throttle:1000})}
function subscribeMode(){if(mode==='navigation'){latched('/map','nav_msgs/OccupancyGrid',m=>renderer.setGrid(m),{compression:'cbor'});topic('/localization/pose','geometry_msgs/PoseWithCovarianceStamped',m=>renderer.setRobot(pose(m.pose.pose)),{throttle:200});topic('/plan','nav_msgs/Path',m=>renderer.setPath(m.poses.map(p=>({x:p.pose.position.x,y:p.pose.position.y}))),{throttle:500});topic('/goal_markers','visualization_msgs/MarkerArray',m=>renderer.setMarkers(m.markers.filter(x=>x.type===0||x.type===2).map(x=>({x:x.pose.position.x,y:x.pose.position.y,color:rgba(x.color)}))))}else if(mode==='preparation'){latched('/web/prep_grid','nav_msgs/OccupancyGrid',m=>renderer.setGrid(m),{compression:'cbor'});topic('/dlio/odom_node/odom','nav_msgs/Odometry',m=>renderer.setRobot(pose(m.pose.pose)),{throttle:200});latched('/web/prep_status','std_msgs/String',m=>{$('prep-status').textContent=m.data;$('finish').disabled=/^(SAVING|CONVERTING)/.test(m.data)})}}
function yaw(q){return Math.atan2(2*(q.w*q.z+q.x*q.y),1-2*(q.y*q.y+q.z*q.z))}function pose(p){return{x:p.position.x,y:p.position.y,yaw:yaw(p.orientation)}}function rgba(c){return`rgba(${c.r*255},${c.g*255},${c.b*255},${c.a})`}
function publishPose(tool,start,end){if(mode!=='navigation')return;let a=Math.atan2(end.y-start.y,end.x-start.x),q={x:0,y:0,z:Math.sin(a/2),w:Math.cos(a/2)},stamp={secs:0,nsecs:0};if(tool==='goal')new ROSLIB.Topic({ros,name:'/goal_pose',messageType:'geometry_msgs/PoseStamped'}).publish({header:{stamp,frame_id:'map'},pose:{position:{x:start.x,y:start.y,z:0},orientation:q}});if(tool==='initial'){let cov=Array(36).fill(0);cov[0]=.25;cov[7]=.25;cov[35]=.0685;new ROSLIB.Topic({ros,name:'/initialpose',messageType:'geometry_msgs/PoseWithCovarianceStamped'}).publish({header:{stamp,frame_id:'map'},pose:{pose:{position:{x:start.x,y:start.y,z:0},orientation:q},covariance:cov}})}renderer.setTool('pan');$('tool').textContent='Tool: pan'}
$('initial').onclick=()=>{renderer.setTool('initial');$('tool').textContent='Tool: initial pose'};$('goal').onclick=()=>{renderer.setTool('goal');$('tool').textContent='Tool: goal'};$('fit').onclick=()=>renderer.fit();$('finish').onclick=()=>{if(!confirm('Save and convert the current map?'))return;$('finish').disabled=true;new ROSLIB.Service({ros,name:'/web/finish_map',serviceType:'std_srvs/Trigger'}).callService({},r=>{if(!r.success)alert(r.message);$('finish').disabled=false})};connect();
